This is a project showing background and foreground execution. It uses nested for loops and runs for the specified amount of time per the file name. No input from the user is required. Simply run the shell script to execute the .cpp files. It uses the Linux time command to exceute the program as a foreground process. Then it uses & to execute it as a background process. The ps command is used to monitor execution in the background. 
